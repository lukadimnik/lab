# this is not needed since we can create load balancer with values.yaml file
apiVersion: v1
kind: Service
metadata:
  labels:
    app: homarr
  name: homarr-dashboard
  namespace: homarr
spec:
  ports:
  - port: 7575
    protocol: TCP
    targetPort: 7575
  selector:
    app.kubernetes.io/instance: homarr
    app.kubernetes.io/name: homarr
  type: LoadBalancer
